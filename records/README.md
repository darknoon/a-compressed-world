Experiment records live here.

Agents should read `results.tsv`, recent `*.json` summaries, and relevant `*.log` files
before changing `train.py`. The files are intentionally plain text so they remain easy to
inspect during long runs.
